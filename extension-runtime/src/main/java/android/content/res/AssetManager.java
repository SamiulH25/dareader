package android.content.res;

/** Minimal dareader-owned AssetManager stub (opaque handle for extension calls). */
public class AssetManager {
    public String[] list(String path) {
        return new String[0];
    }
}
